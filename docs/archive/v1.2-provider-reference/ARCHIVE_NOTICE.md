# Superseded v1.2 provider reference

Historical only. This directory preserves the early `/api/coach` provider-agnostic reference contract.

It is not part of the v2.15.0 FREE production path and must not be deployed for Research Chat. The current backend is `backend/cloudflare-workers-ai/worker.mjs` and the shared current route is `api/research-chat.js`.
