{
  "name": "rms-research-chat-free-cloudflare",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --check worker.mjs && node ../../scripts/deploy-cloudflare-research-chat.mjs --check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": { "wrangler": "^4.102.0" }
}
