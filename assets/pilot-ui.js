window.RMSPilotUI = (()=>{
  const P=window.RMSPilot;
  const esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
  const id=x=>document.getElementById(x);
  const dl=(n,t,type="application/json")=>{const b=new Blob([t],{type}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=n;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)};
  function onboarding(){return `<div class="pilot-section"><h4>Before you begin</h4><div class="onboard-grid"><div><b>1 · Your reasoning first</b><p>Save an independent checkpoint before opening coaching when your teacher wants independent learning evidence.</p></div><div><b>2 · Back up your work</b><p>Your project is stored in this browser. Download a full recovery backup on the schedule set by your class.</p></div><div><b>3 · Do not collect first and ask later</b><p>Human participants, sensitive data, interventions, and hazardous procedures need the required teacher/institutional review before collection.</p></div><div><b>4 · Keep sources and data real</b><p>Do not invent citations, observations, missing values, or statistical results.</p></div><div><b>5 · Research can loop backward</b><p>Literature, pilot observations, or feedback can require a better question, measure, or design.</p></div><div><b>6 · Know what this prototype is</b><p>Local diagnostic scores and competency indicators are instructional and unvalidated. They are not standardized grades.</p></div></div></div>`}
  function backup(project,key){const s=P.storageReport(key);return `<div class="pilot-section"><h4>Backup & recovery</h4><p>Autosave writes to this browser. A downloadable backup protects you from device loss, browser clearing, shared-computer cleanup, or storage failure.</p><div class="pilot-metrics"><div><span>Last local save</span><b>${s.lastSavedAt?esc(new Date(s.lastSavedAt).toLocaleString()):"—"}</b></div><div><span>Project storage</span><b>${(s.primaryBytes/1024).toFixed(1)} KB</b></div><div><span>Recovery snapshot</span><b>${s.recoveryBytes?`${(s.recoveryBytes/1024).toFixed(1)} KB`:"none yet"}</b></div></div><div class="button-row"><button class="primary" id="downloadFullBackup">Download full recovery backup</button><button class="ghost" id="downloadPrivacyBackup">Download privacy-minimized copy</button><label class="ghost file-button">Restore backup<input id="restoreBackup" type="file" hidden accept=".json,application/json"></label></div><div class="pilot-warning"><strong>Full backups may contain sensitive project material.</strong><br>They can include raw data, manuscript text, and source notes. Store and transmit them according to your school or research data-management plan.</div><div id="restoreStatus"></div></div>`}
  function readiness(project,key){const r=P.readiness(project,key),c=project.pilot.config||{};return `<div class="pilot-section"><h4>Classroom pilot readiness</h4><p>This checklist concerns deployment conditions. It does not grant ethics approval.</p><div class="pilot-readiness-head"><div class="audit-score">${Math.max(0,100-r.critical*12-r.warnings*3)}</div><div><b>${r.ready?"Deployment configuration ready for a controlled pilot":"Resolve deployment requirements"}</b><p>${r.critical} critical item(s) · ${r.warnings} optional warning(s)</p></div></div><div class="form-grid two"><label><span>Student identity convention</span><select id="pcIdentity"><option value="">Choose…</option><option value="alias" ${c.identityPolicy==="alias"?"selected":""}>Alias / project code</option><option value="school-approved" ${c.identityPolicy==="school-approved"?"selected":""}>School-approved identifiable naming</option></select></label><label><span>Backup cadence</span><select id="pcBackup"><option value="">Choose…</option>${["every class","weekly","major milestones"].map(x=>`<option ${c.backupCadence===x?"selected":""}>${x}</option>`).join("")}</select></label><label><span>Teacher review process</span><input id="pcTeacher" value="${esc(c.teacherReviewProcess||"")}" placeholder="Example: M1/M3/M4/M5 JSON checkpoint review"></label><label><span>Raw-data storage location</span><input id="pcRaw" value="${esc(c.rawDataLocation||"")}" placeholder="School-approved location; do not enter passwords"></label><label><span>AI policy</span><select id="pcAI"><option value="">Choose…</option><option value="disabled" ${c.aiPolicy==="disabled"?"selected":""}>Disabled for pilot</option><option value="secure-approved-backend" ${c.aiPolicy==="secure-approved-backend"?"selected":""}>Approved secure backend only</option></select></label><label><span>Transfer-task policy</span><select id="pcTransfer"><option value="">Choose…</option><option value="classroom-practice" ${c.transferPolicy==="classroom-practice"?"selected":""}>Public bank for classroom practice</option><option value="disabled" ${c.transferPolicy==="disabled"?"selected":""}>Disabled</option><option value="private-evaluation-bank" ${c.transferPolicy==="private-evaluation-bank"?"selected":""}>Separate private evaluation bank</option></select></label><label class="checkline"><input type="checkbox" id="pcEthics" ${c.ethicsProcess?"checked":""}><span>I have defined how human-participant, privacy, safety, and authority concerns are escalated.</span></label><label class="checkline"><input type="checkbox" id="pcFreeze" ${c.freezeAcknowledged?"checked":""}><span>I will treat the measurement/scoring baseline as frozen during the pilot unless a documented version/deviation is necessary.</span></label></div><button class="secondary" id="savePilotConfig">Save deployment configuration</button>${r.checks.map(x=>`<div class="pilot-check ${x.ok?"ok":x.critical?"bad":"warn"}"><b>${x.ok?"✓":"!"} ${esc(x.label)}</b>${x.note?`<p>${esc(x.note)}</p>`:""}</div>`).join("")}<div class="button-row"><button class="primary" id="startPilot" ${r.ready?"":"disabled"}>Freeze this project to v2.0 pilot baseline</button></div></div>`}
  function baseline(project){const b=P.baselineStatus(project);return `<div class="pilot-section"><h4>Frozen baseline</h4><p>The pilot release separates software improvement from measurement changes. If a measurement-sensitive file changes after pilot start, treat that as a new baseline or documented protocol deviation.</p><div class="baseline-card"><span>Expected baseline</span><b>${esc(b.expected)}</b><span>Project baseline</span><b>${esc(b.projectBaseline)}</b><span>Status</span><b class="${b.matches?"trace-good":"trace-bad"}">${b.matches?"matching":"mismatch"}</b>${b.startedAt?`<span>Pilot started</span><b>${esc(new Date(b.startedAt).toLocaleString())}</b>`:""}</div><div class="pilot-warning"><strong>Measurement files frozen in the release manifest include</strong><br>curriculum, deterministic coach rules, competency model/scoring logic, transfer task bank, transfer logic, methods/statistics/writing diagnostic engines, and stage-prompt definitions.</div><p class="muted tiny">See <code>pilot/FROZEN_BASELINE_MANIFEST_v2.0.json</code> and <code>pilot/BASELINE_FREEZE_POLICY_v2.0.md</code>.</p></div>`}
  function open(project,save,key,active="onboard"){
    P.normalizeProject(project);const wrap=document.createElement("div");wrap.className="modal-backdrop";wrap.id="pilotBackdrop";const tabs=[["onboard","Quick start"],["backup","Backup & recovery"],["readiness","Pilot readiness"],["baseline","Baseline"]];const content=active==="onboard"?onboarding():active==="backup"?backup(project,key):active==="readiness"?readiness(project,key):baseline(project);wrap.innerHTML=`<div class="modal pilot-modal"><div class="journey-head"><div><h3>Pilot & Recovery Center</h3><p>Classroom onboarding, backup protection, deployment checks, and frozen-baseline status.</p></div><button class="ghost small" id="closePilot">Close</button></div><div class="pilot-tabs">${tabs.map(([k,l])=>`<button data-ptab="${k}" class="${active===k?"active":""}">${l}</button>`).join("")}</div>${content}</div>`;document.body.appendChild(wrap);const rr=t=>{wrap.remove();open(project,save,key,t||active)};wrap.onclick=e=>{if(e.target===wrap)wrap.remove()};id("closePilot").onclick=()=>wrap.remove();document.querySelectorAll("[data-ptab]").forEach(b=>b.onclick=()=>rr(b.dataset.ptab));
    if(id("downloadFullBackup"))id("downloadFullBackup").onclick=()=>{const p=P.backupEnvelope(project,"full");dl(P.backupFilename(project,"full"),JSON.stringify(p,null,2))};
    if(id("downloadPrivacyBackup"))id("downloadPrivacyBackup").onclick=()=>{const p=P.backupEnvelope(project,"privacy");dl(P.backupFilename(project,"privacy"),JSON.stringify(p,null,2))};
    if(id("restoreBackup"))id("restoreBackup").onchange=async()=>{const f=id("restoreBackup").files?.[0];if(!f)return;try{const packet=JSON.parse(await f.text()),v=P.validateBackup(packet);if(!v.ok)throw new Error(v.error||"Backup checksum validation failed.");if(!v.restorable)throw new Error(v.error||"This file cannot be used for project recovery.");if(!confirm(`Restore ${v.mode} backup? Current browser project will be replaced.`))return;const restored=P.applyRestore(project,v.project);Object.keys(project).forEach(k=>delete project[k]);Object.assign(project,restored);const saved=save();if(saved?.ok===false)throw new Error("The backup was read, but the restored project could not be saved to browser storage.");id("restoreStatus").innerHTML='<div class="stat-good">Backup restored successfully. Reloading the project…</div>';setTimeout(()=>location.reload(),350)}catch(e){id("restoreStatus").innerHTML=`<div class="coach-feedback bad">${esc(e.message)}</div>`}};
    if(id("savePilotConfig"))id("savePilotConfig").onclick=()=>{project.pilot.config={identityPolicy:id("pcIdentity").value,backupCadence:id("pcBackup").value,teacherReviewProcess:id("pcTeacher").value.trim(),rawDataLocation:id("pcRaw").value.trim(),aiPolicy:id("pcAI").value,transferPolicy:id("pcTransfer").value,ethicsProcess:id("pcEthics").checked,freezeAcknowledged:id("pcFreeze").checked};save();rr("readiness")};
    if(id("startPilot"))id("startPilot").onclick=()=>{try{P.startPilot(project,key);save();rr("baseline")}catch(e){alert(e.message)}};
  }
  function maybeOnboard(project,save,key){if(!project.name&&!P.onboardingSeen()){P.markOnboardingSeen();setTimeout(()=>open(project,save,key,"onboard"),150)}}
  return {open,maybeOnboard};
})();
